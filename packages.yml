# Packages list and a small amount of information goes here.

Web:
    version: 0.0.1
    description: "An extensible web interface and REST API build on Bottle and CherryPy."
    requires:
        modules:
        - "bottle"
        - "cherrypy"
        packages: []
Money:
    version: 0.0.1
    description: "A simple command to convert between currencies."
    requires:
        modules: []
        packages: []
URL-tools:
    version: 0.0.1
    description: "Provides a set of tools for working with the URLs plugin."
    requires:
        modules: []
        packages: []